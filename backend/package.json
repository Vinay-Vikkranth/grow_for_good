{
  "name": "grow-for-good-backend",
  "version": "1.0.0",
  "description": "Express proxy backend for Grow For Good — keeps Anthropic API key server-side",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
